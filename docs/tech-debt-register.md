# Tech Debt Register

> 记录已知技术债与延迟验证项。每条注明来源 story 与归属去向。

- **2026-06-06** (FF-003 异步加载纪律 + Deinitialize CancelHandle): AC-4 真磁盘 DA 的真 deferred-callback UAF 防护**完整验证**留 story-007 PIE 实测 — headless `-nullrhi` 无 tick、deferred completion delegate 不被 `FlushAsyncLoading` pump，transient 内存对象走同步完成路径，无法在 headless 可靠驱动「真异步加载中途 PIE Stop → 悬挂回调写已 GC 宿主」这一真实地雷场景。当前 FF-003 已覆盖 CancelHandle 调用（变异测试坐实）+ TWeakObjectPtr/bIsDeinitializing 双 guard 逻辑；真异步路径验证 = ADR-0001 Verification ③，归 story-007（PIE 隔离验证）。来源 story: production/epics/foundation-framework/story-003-async-load-cancel-handle.md
- **2026-06-06** (BD-001 棋盘数据 schema): 编辑器 `TArray` 列 Details 面板 `+` 填入验证 [Advisory] 手动 — `-nullrhi` headless 不可测，归 story-007 编辑器实测（坐实 ADR-0002 Verification ① CSV TArray 实务限制 / DA 选型），证据落 `production/qa/evidence/`。来源 story: production/epics/board-data/story-001-board-data-asset-schema.md
- **2026-06-06** (BD-001 code-review W-3, propagation → producer): `docs/architecture/control-manifest.md:98` Forbidden 项写「CSV 不支持 TArray 列(5.7 仍报 Unsupported Property type,已核验)」与 ADR-0002 + Sprint0 spike **REFUTED** 结论矛盾（`FArrayProperty` 在 `IsSupportedTableProperty` 白名单、类型层不报该错；不便属实务层：数组字面量 `(a,b,c)` 与逗号冲突）。须改措辞与 ADR-0002 一致——**Never-DataTable 结论本身不变**（实务 + 正向收益独立成立），仅纠正理由。来源 story: production/epics/board-data/story-001-board-data-asset-schema.md
- **2026-06-06** (DR-001 RNG 服务宿主): [Host] PIE Stop→Start 重触发 OnWorldBeginPlay + Seed 重注入的**完整运行时验证** → ff-007（PIE 隔离验证）。headless `-nullrhi` 下 `UWorld::CreateWorld` 不自动触发 BeginPlay，无法可靠驱动「第二局 PIE Start 时 OnWorldBeginPlay 重触发 + Seed 重新注入」。DR-001 已覆盖 headless 可测部分（继承链/ShouldCreate Game+PIE/值语义/注入点 bUseFixedSeed-true 注入 + false-path seed==0 pin）；PIE 运行时重触发 = ADR-0001 Verification + ADR-0004 Verification（OnWorldBeginPlay 在 Stop→Start 重触发），归 ff-007，与 ff-001/ff-003 同惯例。来源 story: production/epics/dice-rng/story-001-rng-service-host-primitives.md
- **2026-06-06** (BD-002 Board DA 持有者宿主): Cast 失败无恢复路径 → **story-005**。`UBoardDataAssetHost::OnDataAssetLoaded` 中 `Cast<UBoardDataAsset>` 失败（加载到非 board 的 UPrimaryDataAsset）时停在 Loaded 态、不推进 Active，但**无恢复路径**（基类 `RequestReset` 需 Active 态才能重置，Loaded 态无出口）。当前是类型安全守卫（正常 typed seam 下 Cast 不会失败），TC_L2d 已 pin 此行为。正式失败态表示与恢复路径 = story-005 的 LoadFailed 设计决策：方案 A 新增 `EHostLoadState::LoadFailed`（改基类 DataAssetHostSubsystem.h）/ 方案 B 沿用 Empty + 结构化错误码。来源 story: production/epics/board-data/story-002-da-holder-subsystem-lifecycle.md
- **2026-06-06** (BD-002 code-review, ADR 措辞 reconcile → producer, minor): ADR-0002 §Implementation Guidelines #2「建议 UAssetManager 同步加载；异步仅未来多棋盘预加载场景」与 ff-003/bd-002 实际采用的**异步** holder（RequestAsyncLoad + CancelHandle）措辞不一致。实现合规（story 主 ADR-0001 显式 prescribe 异步 + CancelHandle，AC-L3 需要；ADR-0002「同步即可」是允许非强制），非违规。建议 producer 更新 ADR-0002 guideline-2 措辞，注明 FF-003 选异步 holder 是为 CancelHandle/PIE-safety，sync 被评估为足够但未采用。来源 story: production/epics/board-data/story-002-da-holder-subsystem-lifecycle.md
- **2026-06-07** (BD-006 加载期校验警告类, AC-24a 显示回退 → HUD/显示层): AC-24a 第二从句「`GetTileData` 回退显示 `"Tile {index}"`（DisplayName 为空时）」**未实现且非 bd-006 校验器职责**。bd-006 交付 EmptyDisplayName 警告码（已测）；但回退「显示」是显示层行为——核实 `GetTileData`（story-004 done）返回 raw `FBoardTileData` struct，不合成显示名；且「Tile {index}」是玩家可见字符串，硬编码进数据访问器违反「无硬编码玩家串/本地化」编码标准，应是 HUD 的本地化格式串。归属：HUD epic（#16，呈现层，Sprint0 未实现）的 DisplayName 渲染回退，或未来 `GetTileDisplayName` 本地化访问器。不在 bd-006 修复（避免越权改已批 story-004 + 错层）。来源 story: production/epics/board-data/story-006-load-validation-warnings.md
- **2026-06-07** (DR-003 code-review, logged-decision propagation → producer): `docs/architecture/control-manifest.md` §确定性 RNG (ADR-0004) + `ADR-0004` §Key Interfaces 仍携 stale 文本「`Min>Max → ensure(Min<=Max)+UE_LOG(Warning)`」「封装层 `ensure((int64)Max-(int64)Min+1 < (1<<24))`」。但既定 logged decision「验证错误路径用 `UE_LOG(LogTemp,Error)` 替 `ensure()`」（headless Automation 中 ensure callstack 不稳定致 AddExpectedError 计数不可靠）已横跨 FF-003→DR-001 AC-34→BD-003→BD-004 AC-27→BD-005→**DR-003** 应用 6+ 次，但每个 story 仅 doc-sync 自身 AC，**从未传播至 manifest/ADR 源**。须 producer 统一更新 control-manifest §确定性 RNG + ADR-0004 Key Interfaces 伪码：`ensure(...)+UE_LOG(Warning)` → `UE_LOG(LogTemp,Error)`（保留语义：返回 Min 不 swap / Range≥2^24 Error+直通）。结论本身不变，仅纠正实现手段措辞与既定惯例一致。来源 story: production/epics/dice-rng/story-003-randomrange-degenerate-boundary.md
- **2026-06-07** (FF-004 GameInstance 宿主, AC-2/5/6 headless 验证边界 → ff-007 + bd-007): `UPersistentServicesSubsystem`（跨局 UGameInstanceSubsystem 宿主）的 3 个核心运行时 AC 不可在 headless `-nullrhi` 验证（GameInstance::Init 需完整引擎栈/GEngine/LocalPlayer/FWorldContext，headless 不可靠；GetSubsystem 静默 null）——本 story 用结构反射覆盖 AC-1/3/4（IsChildOf+FindFunctionByName+TFieldIterator，5 测试全绿），3 运行时 AC 诚实 DEFER（无 false-green 假断言）：① **AC-2** StartNewGame 跨 PIE 局存活 → ff-007（需 PIE Start→Stop→Start）② **AC-6** PIE 三局宿主进程单例 → ff-007 ③ **AC-5** UAssetManager 缓存 DA_Board 跨局不释放 → ff-007 + **bd-007**（需真 DA_Board 资产，bd-007 编辑器创作后方可验跨局缓存无第二次磁盘 IO）。ff-007/bd-007 解锁后补 TC-1/2/3/5 真运行时验证。来源 story: production/epics/foundation-framework/story-004-game-instance-subsystem-host.md
- **2026-06-07** (FF-004 StartNewGame 最小 seam → pt-001/回合 epic): `FGameSetupConfig`（`Source/rento/GameSetupConfig.h`）是空 USTRUCT 最小 seam，仅为冻结 `StartNewGame(const FGameSetupConfig&)` 入口名（ADR-0001 Impl Note 3「接口名冻结，本 story 只定宿主」）。**pt-001（回合 epic StartNewGame 开局入口）须在此 struct 上填充字段（PlayerCount/RandomSeed/BoardAsset 等）+ 填充 `UPersistentServicesSubsystem::StartNewGame` body，禁止重声明同名 struct 或同名入口**（占位注释已在 GameSetupConfig.h/PersistentServicesSubsystem.h 标明）。pt-001 依赖 ff-004，开工时回读此 seam。来源 story: production/epics/foundation-framework/story-004-game-instance-subsystem-host.md
- **2026-06-07** (FF-002 code-review, AC-2 headless 验证边界 → ff-007): TC-1 验「`FWorldGameClock::NowSeconds()` 读 World time」在 `-nullrhi` headless 下 World 时间恒静止 0.0，`TestEqual(ClockTime, WorldTime)` = `0.0==0.0` 无法证伪「返回常量 0.0」的假实现（qa-tester 变异核验坐实 vacuous）。TC-1 已 honest 降级仅验接口实现+默认时钟读数一致+nullptr 兜底；**AC-2「读 LIVE 推进 World 时间」完整证明归 ff-007（PIE 真 tick 推进 World 时间后断言 FWorldGameClock 跟随）**，与 FF-003 AC-4 真磁盘 UAF / DR-001 PIE 重触发同 headless 边界惯例。注：本 story 的 DI 核心契约（AC-4/5/6 注入生效）由 TC-3 非 vacuous 完整覆盖（变异坐实），AC-2 仅是生产时钟薄包装的 live-time 读取验证延迟。来源 story: production/epics/foundation-framework/story-002-igameclock-di.md
- **2026-06-07** (FF-002 code-review, ADR-0008 §Key Interfaces 跨 ADR 不一致 → producer + HUD epic): ADR-0008 §Key Interfaces (L209-235) 定义 `IGameClock` 方法为 `GetGameTimeSec()` + `GetUnpausedTimeSec()`（`float`），但 story AC-1 + ADR-0001 §Key Interfaces (L177)（本 story primary ADR）定义为 `NowSeconds()`（`double`）。FF-002 忠实 primary（ADR-0001+AC-1）实现 `NowSeconds()`，`GetUnpausedTimeSec` 显式 Out of Scope（HUD 专用）。两点须 producer/HUD epic 处理：① ADR-0008 §Key Interfaces 方法名/返回类型措辞 stale（应与 as-built `NowSeconds() double` 对齐，或注明 HUD 扩展方法）；② **HUD `unpaused_elapsed`（ADR-0008 L163，暂停不计的游戏时间）是未来 IGameClock 扩展义务——当前 `NowSeconds()` 读 raw World time ≠ 暂停不计时间**，HUD/Presentation epic 须扩展 IGameClock（加 GetUnpausedTimeSec 或生产时钟加暂停追踪），勿误把 NowSeconds 当 unpaused 时间用。来源 story: production/epics/foundation-framework/story-002-igameclock-di.md
- **2026-06-07** (BD-007 code-review, 5 项 logged-decision/债): bd-007 两审（unreal-specialist+qa-tester）0 真 BLOCKING，3 should-fix 已应用闭合（cook-gate fail-closed / MarkPackageDirty / TC_AC6b 经 GetTilesInGroup 验真资产）+ 1 验证驱动新发现已修（generator 幂等覆盖崩溃，FullyLoad+复用清空）。以下 deflate 为非阻塞债：① **W-1（架构债→加 Shipping target 前）**：两 commandlet（`GenerateClassic40`/`BoardCookGate`）整个 UCLASS 用 `#if WITH_EDITOR` 包裹放在 `rento` 运行时模块。Sprint0 仅 editor target 编译实测无碍，但 UE 官方模式是 editor-only commandlet 住独立 `*Editor` 模块（如 Epic 的 ResavePackagesCommandlet）。加 Game/Shipping target 前应迁至独立 `rentoEditor` 模块（Build.cs `if(Target.bBuildEditor)`），用模块边界而非 `#if` 控制可见性。② **W-4（minor）**：`GenerateClassic40Commandlet::Main` ~345 行（40 格逐行 Add）；可重构为文件顶部 `static const` 行数组 + dispatch 压到 ~80 行，留经济 epic 覆盖真值时一并做。③ **I-2（项目级）**：commandlet 用 `LogTemp`；全项目现状一致，建议统一声明 `DECLARE_LOG_CATEGORY(LogRento)` 替换（利 CI 日志过滤），项目级债非单 story。④ **GAP-1/3 → 经济 epic**：cook-gate 的 `bIsPlaceholderData==false → 放行(exit 0)` 路径 + TC_AC4c 的 false 期望尚无自动化覆盖（false 资产尚不存在）；经济(5) 将字段置 false 时自然提供 false 资产，届时补 cook-gate false→0 test + 翻转 TC_AC4c 期望。⑤ **INFO-4 → 未来 OQ**：多 board 资产混合（一真一假）时 cook-gate 会因任一占位 return 1 阻断；MVP 单资产无碍，增自定义棋盘资产后须细化 gate 作用域。来源 story: production/epics/board-data/story-007-classic40-asset-config-harness.md
- **2026-06-07** (BD-007 → devops-engineer, CI 排序): bd-007 的 `[Config]` harness + cook-gate 是 **nightly/staging gate，非 PR merge gate**（需 editor/commandlet 环境，且依赖资产落盘）。CI pipeline 须保证：① `BoardCookGate` commandlet 在 `GenerateClassic40` 成功后跑（否则资产不存在；现已 fail-closed return 1，但顺序保证仍利于清晰定位）；② cook-gate 跑前 AssetRegistry 缓存为最新（GenerateClassic40 刚落盘的资产须被扫到，避免 stale 缓存漏检）；③ Alpha 里程碑 CI 将 cook-gate exit 1 视为硬失败，MVP/nightly 忽略 exit 1。来源 story: production/epics/board-data/story-007-classic40-asset-config-harness.md
- **2026-06-07** (FF-002 IGameClock DI, 路径 A 裁定 propagation → producer): `IGameClock` 指针承载形态——`docs/architecture/control-manifest.md` §Foundation/Global + `ADR-0008` 的 specialist finding 措辞「`IGameClock*` 裸指针 GC → `TWeakObjectPtr`/`TScriptInterface`」**假设 IGameClock 是 UObject**，但 ADR-0008 §Key Interfaces (L209-235) + ADR-0001 §Key Interfaces (L177) 自身把 `IGameClock`/`FWorldGameClock`/`FMockGameClock` 定义为**纯 C++ 接口/类（非 UObject）**——`TScriptInterface`/`TWeakObjectPtr` 仅对 UObject 派生类型成立，自相矛盾；且若做成 UObject 成员挂宿主基类会回归打破 FF-001 TC-4「`UMatchSubsystemBase` 本类 UPROPERTY==0」。**FF-002 已用路径 A 兑现**（msc 2026-06-07 裁定）：宿主以 `TSharedPtr<IGameClock>` 承载（非裸、引用计数安全、非 UPROPERTY 保 TC-4），`FWorldGameClock` 对 `UWorld` 仍用 `TWeakObjectPtr<UWorld>`（finding 在真 UObject 引用处兑现）。须 producer 校正 control-manifest Global + ADR-0008 specialist-finding 措辞为 `TSharedPtr<IGameClock>`，使下游 HUD/Presentation epic 消费方不再重导 UObject 形态。结论（非裸指针安全）不变，仅纠正承载类型措辞。来源 story: production/epics/foundation-framework/story-002-igameclock-di.md
